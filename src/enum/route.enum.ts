export enum dogsApiLink {
  GET_ALL = '/dogs',
  GET_ONE = '/dog/:id',
  CREATE = '/dog',
  DELETE = '/dog/:id',
  UPDATE = '/dog/:id'
}

export enum apiRoot {
  ROOT = '/api'
}

export enum pingApi {
  PING = '/ping'
}